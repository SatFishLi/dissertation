The code for the models is partially duplicated and could have used encapsulation to improve modularity.
But here it was intended to tune the parameters or optimise each model, so it was split into 5 different methods to differentiate them.
But due to time constraints, this didn't get implemented.